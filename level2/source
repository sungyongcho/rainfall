#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void  p()
{
  char a[64];
  void* b;

  fflush(stdout);

  b = gets(a);
  if ((0xb000000 & (int)b) != 0xb0000000)
  {
    printf("(%p)\n", a);
    exit(1);
  }
  puts(a);
  strdup(a);
  return;
}

int main()
{
  p();
  return(0);
}
